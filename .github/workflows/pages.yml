name: Pages
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: npm install && npm run docs:build
      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@releases/v3
        with:
          ACCESS_TOKEN: ${{ secrets.DOCS }} # 生成的 secret
          BRANCH: gh-pages # 部署到 gh-pages 分支
<<<<<<< HEAD
          FOLDER: docs/.vuepress/dist/ # vuepress 生成的静态文件存放的地方
=======
          FOLDER: docs/docs/.vuepress/dist/ # vuepress 生成的静态文件存放的地方
>>>>>>> a34b1908adbd4b0cd83f76f6f580acc8a3692393
